# Pinned to versions the analysis has been run under.
# Any reasonably recent versions of these four work; scipy >= 1.7 is required
# for scipy.optimize.minimize(method="L-BFGS-B") with the options used here.
numpy>=1.21
scipy>=1.7
pandas>=1.3
matplotlib>=3.4
jupyter>=1.0
